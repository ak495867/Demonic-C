import "parser";

fn main() -> int {
    var fh: int = file_open("parser.dmc", "rb");
    if (fh < 0) {
        return 99;
    }
    let src = file_read(fh);
    file_close(fh);
    var ts: [Tok; 1024] = [Tok{kind: 0, text: "", line: 0}];
    let count = lex(src, &ts[0], 1024);
    var ds: [Decl; 32] = [Decl{kind: 0, name: "", start: 0, end: 0}];
    let n = parse_top(ts, count, &ds[0], 32);
    println(n);
    var i: int = 0;
    while (i < n) {
        println(ds[i].kind);
        println(ds[i].name);
        i = i + 1;
    }
    return n;
}
