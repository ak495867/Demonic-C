# Standard Library

Demonic C exposes JSON, HTTP, datetime, regex, and database entry points through the compiler-generated runtime. The current implementations provide deterministic local behavior for JSON text, time formatting, regular-expression matching, and extension points for network and database adapters.
